#ifndef CALCULATOR_H
#define CALCULATOR_H

class Calculator
{
    public:
        int Add (double, double);
        int Sub (double, double);
        int Add (int, int);
        int Mul (double, double);
        int Pow (double);
};

#endif//CALCULATOR_H
